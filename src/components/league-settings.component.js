import React, {Component} from "react";
export default class LeagueSettings extends Component{
    render(){
        return(
            <div className="league-settings-panel">
                <h1>you are in league settings</h1>
            </div>
        );
    }
}